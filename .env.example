# Copy this file to backend/.env and add a valid key to enable Groq responses.
# Without a key the application stays fully usable in clearly marked local mode.
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile
